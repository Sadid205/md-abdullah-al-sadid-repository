services:
  frontend:
    build:
      network: host
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
